/* Sample/demo inductions removed — this tool now shows only real submissions.
   The dashboard reads real data from the shared store (server) or this device's
   localStorage. If you ever want demo data back, restore this file from git history
   (commit before the sample data was removed). */
window.SAMPLE_INDUCTIONS = [];
